import s from "./App.module.scss";
import React from 'react';

export const Footer = () => {
    return (
        <footer className={s.footer}>
            this is footer
        </footer>
    );
};


